name: Security Checks

on:
  push:
    branches: master
    paths:
      - 'cmd/konvey/**'
      - 'internal/konvey/**'
  pull_request:
    branches: master
  workflow_dispatch:

jobs:
  sec:
    uses: kowabunga-cloud/workflows/.github/workflows/go-sec.yml@master
    secrets: inherit
